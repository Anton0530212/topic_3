# Программа, которая запрашивает у
# пользователя две строки и объединяет их в одну,
# разделяя символом -(дефис).
line_1: str = input('Введите первую строку: ')
line_2: str = input('Введите первую строку: ')

result = line_1 + '-' + line_2

print('Результат:', result)
